spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${cola.nacos.server.url}
      config:
        server-addr: ${cola.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}
